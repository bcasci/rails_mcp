# ADR-NNNN — <short decision title>

Status: Proposed | Accepted (YYYY-MM-DD) | Superseded by ADR-XXXX | Deprecated

## Context

The forces at play: the problem, the constraints, the options considered. Write this at
decision time so the "why" is real, not reconstructed later.

## Decision

The choice, in active voice. One decision per ADR.

## Consequences

What becomes easier, what becomes harder, what is now constrained. If this is a standing
constraint, name the machine-checkable rule (the grep the pre-commit / CI enforces).

---
Immutable once Accepted. To change the decision, add a NEW ADR that supersedes this one, set
this file's Status to "Superseded by ADR-XXXX", and link both ways. Never rewrite the
Decision or Context of an accepted ADR — only its status, links, and typo fixes.
