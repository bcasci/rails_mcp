# frozen_string_literal: true

# Pins the Rails 8.0 line for the CI matrix (CI-01). Reuses the root Gemfile's
# dev deps and the gemspec; selected via BUNDLE_GEMFILE.
eval_gemfile "../Gemfile"

gem "rails", "~> 8.0.0"
gem "railties", "~> 8.0.0"
gem "activesupport", "~> 8.0.0"
